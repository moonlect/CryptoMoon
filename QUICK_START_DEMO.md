# 🚀 Быстрый запуск MVP для демонстрации

## Минимальные требования

- Docker и Docker Compose
- Python 3.11+
- Node.js 18+
- npm или pnpm

## Запуск за 3 шага

### 1. Настройка

```bash
# Создайте .env файл
cp .env.example .env

# Отредактируйте .env, минимум укажите:
# SECRET_KEY=your-random-secret-key-min-32-chars
```

### 2. Запуск Docker сервисов

```bash
docker-compose up -d postgres redis
```

### 3. Запуск приложений

**Windows:**
```cmd
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

Или вручную:

**Терминал 1 (Backend):**
```bash
pip install -e ".[dev]"
alembic upgrade head
uvicorn app.main:app --reload
```

**Терминал 2 (Frontend):**
```bash
cd frontend
npm install
npm run dev
```

## ✅ Готово!

Откройте http://localhost:5173

## 🎯 Быстрая демонстрация

1. Зарегистрируйтесь
2. Просмотрите Market Data
3. Обновитесь до VIP (Pricing → Upgrade)
4. Откройте страницы сигналов
5. Настройте уведомления в Profile

## 📝 Примечания

- Для реальных данных CoinMarketCap нужен API ключ
- Telegram бот опционален для демо
- Email уведомления опциональны

**Все основные функции работают без внешних сервисов!**



