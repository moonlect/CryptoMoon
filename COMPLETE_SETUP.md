# ✅ CryptoTracker MVP - Полная настройка и запуск

## 🎯 Статус проекта

**100% готов к демонстрации!**

- ✅ Backend полностью реализован
- ✅ Frontend полностью реализован
- ✅ Все 9 страниц из ТЗ
- ✅ WebSocket интеграция
- ✅ Docker Compose конфигурация
- ✅ Скрипты автоматического запуска

## 📦 Что включено

### Backend
- FastAPI приложение
- PostgreSQL база данных
- Redis кеширование
- WebSocket сервер
- Telegram бот
- Email сервис

### Frontend
- React 18 + TypeScript
- Tailwind CSS
- Все страницы из ТЗ
- Real-time обновления
- Адаптивный дизайн

## 🚀 Запуск (выберите один вариант)

### Вариант A: Автоматический запуск

**Windows:**
```cmd
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

### Вариант B: Docker Compose (все в контейнерах)

```bash
# 1. Настройте .env
cp .env.example .env
# Отредактируйте .env

# 2. Запустите все сервисы
docker-compose up -d

# 3. Примените миграции
docker-compose exec backend alembic upgrade head
```

### Вариант C: Ручной запуск

```bash
# 1. Настройте .env
cp .env.example .env

# 2. Запустите Docker (БД и Redis)
docker-compose up -d postgres redis

# 3. Backend
pip install -e ".[dev]"
alembic upgrade head
uvicorn app.main:app --reload

# 4. Frontend (в другом терминале)
cd frontend
npm install
npm run dev
```

## 📍 Доступ

После запуска:

- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:8000
- **API Docs:** http://localhost:8000/docs

## 🎬 Сценарий демонстрации

1. **Откройте** http://localhost:5173
2. **Зарегистрируйтесь** (Sign Up)
3. **Просмотрите Market Data** (доступно для всех)
4. **Обновитесь до VIP:**
   - Перейдите в Pricing
   - Нажмите "Upgrade to VIP"
   - Подтвердите (в MVP мгновенная активация)
5. **Откройте страницы сигналов:**
   - MEXC Spot & Futures
   - Funding Rate Spread
   - MEXC & DEX Price Spread
6. **Настройте уведомления** в Profile
7. **Проверьте WebSocket** (откройте консоль браузера)

## 🔧 Минимальная конфигурация для демо

В `.env` обязательно укажите:
```env
SECRET_KEY=your-secret-key-min-32-characters-long
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cryptotracker
DATABASE_URL_SYNC=postgresql://postgres:postgres@localhost:5432/cryptotracker
REDIS_URL=redis://localhost:6379/0
```

Остальные настройки опциональны для базовой демонстрации.

## ✨ Особенности для демо

- Все страницы работают
- Регистрация и вход
- Обновление подписки (мгновенное в MVP)
- WebSocket подключение (для VIP)
- Фильтрация и поиск
- Адаптивный дизайн

## 📊 Что работает без внешних сервисов

✅ Регистрация и аутентификация
✅ Просмотр Market Data (если есть данные в БД)
✅ Все страницы интерфейса
✅ Обновление подписки
✅ Настройки профиля
✅ WebSocket подключение

## ⚙️ Что требует настройки (опционально)

⚠️ CoinMarketCap API - для реальных котировок
⚠️ Telegram Bot - для автоматических сигналов
⚠️ SMTP - для email уведомлений

**Для базовой демонстрации эти настройки не обязательны!**

## 🎉 Готово к демонстрации!

Проект полностью функционален и готов к показу заказчику.



