# 📖 Полное руководство по запуску CryptoTracker MVP локально

> **Важно:** Это руководство покрывает все шаги от установки до запуска проекта. Если вы уже знакомы с Python, Node.js и Docker, используйте раздел [Быстрый старт](#быстрый-старт-для-опытных). Для новичков рекомендуется следовать всем шагам по порядку.

## Содержание

1. [Быстрый старт (для опытных)](#быстрый-старт-для-опытных)
2. [Предварительные требования](#предварительные-требования)
3. [Установка зависимостей](#установка-зависимостей)
4. [Настройка окружения](#настройка-окружения)
5. [Настройка базы данных](#настройка-базы-данных)
6. [Запуск Backend](#запуск-backend)
7. [Запуск Frontend](#запуск-frontend)
8. [Проверка работоспособности](#проверка-работоспособности)
9. [Решение проблем](#решение-проблем)
10. [Дополнительная настройка](#дополнительная-настройка)

---

## Быстрый старт (для опытных)

Если вы уже знакомы с Python, Node.js и Docker, выполните эти команды:

```bash
# 1. Создайте и активируйте виртуальное окружение
python -m venv venv
# Windows: venv\Scripts\activate
# Linux/Mac: source venv/bin/activate

# 2. Установите зависимости
pip install --upgrade pip
pip install -e ".[dev]"

# 3. Создайте .env файл (см. раздел "Настройка окружения")
# Минимум: SECRET_KEY, DATABASE_URL, DATABASE_URL_SYNC, REDIS_URL

# 4. Запустите Docker сервисы
docker-compose up -d postgres redis

# 5. Примените миграции
alembic upgrade head

# 6. Запустите backend (в одном терминале)
uvicorn app.main:app --reload

# 7. Запустите frontend (в другом терминале)
cd frontend
npm install
echo "VITE_API_URL=http://localhost:8000" > .env
npm run dev
```

**Готово!** Откройте http://localhost:5173

**Если что-то не работает**, читайте подробные инструкции ниже.

---

## Предварительные требования

### 1. Проверка установленного ПО

#### Python 3.11 или выше

**Windows:**
```cmd
python --version
```
Должно показать: `Python 3.11.x` или выше

Если не установлен:
- Скачайте с https://www.python.org/downloads/
- При установке отметьте "Add Python to PATH"
- Перезапустите терминал

**Linux:**
```bash
python3 --version
# или
python3.11 --version
```

Если не установлен:
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3.11 python3.11-venv python3.11-pip

# Fedora
sudo dnf install python3.11
```

**Mac:**
```bash
python3 --version
```

Если не установлен:
```bash
# Через Homebrew
brew install python@3.11
```

#### Node.js 18 или выше

**Проверка:**
```bash
node --version
npm --version
```

Должно показать: `v18.x.x` или выше

Если не установлен:
- Windows/Mac: Скачайте с https://nodejs.org/
- Linux:
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Fedora
sudo dnf install nodejs npm
```

#### Docker и Docker Compose

**Проверка:**
```bash
docker --version
docker-compose --version
```

Если не установлен:
- Windows/Mac: Скачайте Docker Desktop с https://www.docker.com/products/docker-desktop
- Linux:
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install docker.io docker-compose

# Запустите Docker
sudo systemctl start docker
sudo systemctl enable docker

# Добавьте пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER
# Выйдите и войдите снова
```

#### Git (опционально, но рекомендуется)

```bash
git --version
```

---

## Установка зависимостей

### Шаг 0: Проверка структуры проекта

Убедитесь, что в корне проекта есть следующие файлы и папки:

**Обязательные файлы:**
- `pyproject.toml` - конфигурация Python проекта
- `docker-compose.yml` - конфигурация Docker сервисов
- `alembic.ini` - конфигурация миграций
- `app/` - папка с backend кодом
- `frontend/` - папка с frontend кодом

**Проверка (Windows):**
```cmd
dir pyproject.toml
dir docker-compose.yml
dir app
dir frontend
```

**Проверка (Linux/Mac):**
```bash
ls -la pyproject.toml docker-compose.yml
ls -d app frontend
```

Если каких-то файлов нет, убедитесь, что вы находитесь в правильной директории проекта.

### Шаг 1: Клонирование/получение проекта

Если проект в Git:
```bash
git clone <repository-url>
cd CryptoMoon
```

Если проект уже есть локально:
```bash
cd C:\Users\user\PycharmProjects\CryptoMoon
```

### Шаг 2: Создание виртуального окружения Python

**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации в начале строки терминала должно появиться `(venv)`.

**Важно:** Виртуальное окружение нужно активировать каждый раз при открытии нового терминала!

### Шаг 3: Установка зависимостей Backend

Убедитесь, что виртуальное окружение активировано, затем:

```bash
# Обновите pip
pip install --upgrade pip

# Установите зависимости проекта
pip install -e ".[dev]"
```

Это установит все зависимости из `pyproject.toml`, включая:
- FastAPI
- SQLAlchemy
- Redis клиент
- Telegram bot
- И все остальные зависимости

**Ожидаемое время:** 2-5 минут

### Шаг 4: Установка зависимостей Frontend

Откройте новый терминал (виртуальное окружение не нужно):

```bash
cd frontend
npm install
```

Или если используете pnpm:
```bash
pnpm install
```

**Ожидаемое время:** 1-3 минуты

---

## Настройка окружения

### Шаг 1: Создание файла .env

**Вариант 1: Если есть .env.example**

**Windows:**
```cmd
copy .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

**Вариант 2: Если .env.example отсутствует**

Создайте файл `.env` в корне проекта вручную (в той же папке, где находится `pyproject.toml`).

**Windows (PowerShell):**
```powershell
New-Item -Path .env -ItemType File
```

**Windows (CMD):**
```cmd
type nul > .env
```

**Linux/Mac:**
```bash
touch .env
```

Затем откройте файл в текстовом редакторе и заполните его содержимым из следующего шага.

**Пример минимального .env файла (скопируйте и вставьте):**

```env
# ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ
SECRET_KEY=your-secret-key-minimum-32-characters-long-random-string-change-this
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cryptotracker
DATABASE_URL_SYNC=postgresql://postgres:postgres@localhost:5432/cryptotracker
REDIS_URL=redis://localhost:6379/0

# ОПЦИОНАЛЬНЫЕ (можно оставить пустыми для базовой работы)
COINMARKETCAP_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@cryptotracker.com
```

### Шаг 2: Редактирование .env

Откройте файл `.env` в текстовом редакторе и заполните следующие обязательные параметры:

#### Обязательные параметры:

```env
# SECRET_KEY - ОБЯЗАТЕЛЬНО! Минимум 32 символа
# Сгенерируйте случайную строку:
# Windows PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
# Linux/Mac: openssl rand -hex 32
SECRET_KEY=your-secret-key-minimum-32-characters-long-random-string

# Database URLs
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cryptotracker
DATABASE_URL_SYNC=postgresql://postgres:postgres@localhost:5432/cryptotracker

# Redis
REDIS_URL=redis://localhost:6379/0
```

#### Опциональные параметры (для полной функциональности):

```env
# CoinMarketCap API (для реальных котировок)
COINMARKETCAP_API_KEY=your-api-key-here

# Telegram Bot (для автоматических сигналов)
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id

# Email (SMTP) для уведомлений
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@cryptotracker.com
```

**Важно:** 
- `SECRET_KEY` должен быть уникальным и секретным
- Для Gmail используйте App Password, а не обычный пароль
- Telegram бот опционален для базовой демонстрации

### Шаг 3: Генерация SECRET_KEY

**Windows PowerShell:**
```powershell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
```

**Linux/Mac:**
```bash
openssl rand -hex 32
```

**Python:**
```python
import secrets
print(secrets.token_urlsafe(32))
```

Скопируйте сгенерированную строку в `.env` как значение `SECRET_KEY`.

---

## Настройка базы данных

### Шаг 1: Запуск Docker сервисов

Убедитесь, что Docker Desktop запущен (Windows/Mac) или Docker daemon работает (Linux).

```bash
docker-compose up -d postgres redis
```

Эта команда:
- Скачает образы PostgreSQL 15 и Redis 7 (если их нет)
- Создаст контейнеры
- Запустит их в фоновом режиме
- Создаст volumes для постоянного хранения данных

**Проверка статуса:**
```bash
docker-compose ps
```

Должны быть запущены:
- `cryptotracker_postgres` (статус: Up)
- `cryptotracker_redis` (статус: Up)

**Проверка логов:**
```bash
docker-compose logs postgres
docker-compose logs redis
```

### Шаг 2: Ожидание готовности сервисов

Подождите 5-10 секунд, чтобы PostgreSQL полностью запустился.

**Проверка подключения к PostgreSQL:**
```bash
# Windows (если установлен psql)
psql -h localhost -U postgres -d cryptotracker

# Linux/Mac
psql -h localhost -U postgres -d cryptotracker

# Или через Docker
docker-compose exec postgres psql -U postgres -d cryptotracker
```

Пароль: `postgres`

Если подключение успешно, введите `\q` для выхода.

**Проверка Redis:**
```bash
docker-compose exec redis redis-cli ping
```

Должно вернуть: `PONG`

### Шаг 3: Применение миграций базы данных

Убедитесь, что виртуальное окружение Python активировано:

```bash
# Проверка текущей версии миграций
alembic current

# Применение всех миграций
alembic upgrade head
```

Эта команда создаст все таблицы в базе данных:
- users
- subscriptions
- user_preferences
- signals_mexc_spot_futures
- signals_funding_rate
- signals_mexc_dex
- notifications
- coinmarketcap_data
- audit_logs

**Проверка создания таблиц:**
```bash
docker-compose exec postgres psql -U postgres -d cryptotracker -c "\dt"
```

Должны отобразиться все 9 таблиц.

---

## Запуск Backend

### Шаг 1: Активация виртуального окружения

**Windows:**
```cmd
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### Шаг 2: Проверка установки зависимостей

```bash
python -c "import fastapi; print('FastAPI OK')"
python -c "import sqlalchemy; print('SQLAlchemy OK')"
python -c "import redis; print('Redis OK')"
```

Если все команды выводят "OK", зависимости установлены правильно.

### Шаг 3: Запуск сервера

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Флаги:
- `--reload` - автоматическая перезагрузка при изменении кода
- `--host 0.0.0.0` - доступ с любого IP
- `--port 8000` - порт 8000

**Ожидаемый вывод:**
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

**Если видите ошибки:**

1. **Ошибка подключения к БД:**
   ```
   Проверьте, что PostgreSQL запущен: docker-compose ps
   Проверьте DATABASE_URL в .env
   ```

2. **Ошибка подключения к Redis:**
   ```
   Проверьте, что Redis запущен: docker-compose ps
   Проверьте REDIS_URL в .env
   ```

3. **ModuleNotFoundError:**
   ```
   Убедитесь, что виртуальное окружение активировано
   Переустановите зависимости: pip install -e ".[dev]"
   ```

**Ошибки, которые можно игнорировать (если сервисы не настроены):**

- **Telegram bot errors** - если `TELEGRAM_BOT_TOKEN` не указан, это нормально
- **CoinMarketCap errors** - если `COINMARKETCAP_API_KEY` не указан, синхронизация просто не будет работать
- **SMTP errors** - если SMTP не настроен, email уведомления просто не будут отправляться

Эти ошибки не критичны для базовой работы приложения.

### Шаг 4: Проверка Backend

Откройте в браузере:
- http://localhost:8000 - должно показать `{"message": "CryptoTracker API", ...}`
- http://localhost:8000/docs - должна открыться Swagger документация
- http://localhost:8000/health - должно показать `{"status": "healthy"}`

**Проверка через curl:**
```bash
# Windows (PowerShell)
Invoke-WebRequest -Uri http://localhost:8000/health

# Linux/Mac
curl http://localhost:8000/health
```

---

## Запуск Frontend

### Шаг 1: Переход в папку frontend

Откройте **новый терминал** (backend должен продолжать работать в первом терминале):

```bash
cd frontend
```

### Шаг 2: Проверка установки зависимостей

```bash
npm list --depth=0
```

Или проверьте наличие `node_modules`:
```bash
ls node_modules  # Linux/Mac
dir node_modules  # Windows
```

Если `node_modules` отсутствует или неполный:
```bash
npm install
```

### Шаг 3: Настройка переменных окружения Frontend

Создайте файл `frontend/.env` в папке `frontend` (не в корне проекта!).

**Windows (PowerShell):**
```powershell
cd frontend
echo "VITE_API_URL=http://localhost:8000" | Out-File -FilePath .env -Encoding utf8
cd ..
```

**Windows (CMD):**
```cmd
cd frontend
echo VITE_API_URL=http://localhost:8000 > .env
cd ..
```

**Linux/Mac:**
```bash
cd frontend
echo "VITE_API_URL=http://localhost:8000" > .env
cd ..
```

**Или создайте файл вручную:**

1. Откройте папку `frontend`
2. Создайте новый файл с именем `.env` (с точкой в начале!)
3. Добавьте в него одну строку:
   ```
   VITE_API_URL=http://localhost:8000
   ```

**Важно:** 
- Файл должен быть в папке `frontend`, а не в корне проекта
- После изменения `.env` перезапустите dev сервер!
- Если используете другой порт для backend, измените URL соответственно

### Шаг 4: Запуск dev сервера

```bash
npm run dev
```

**Ожидаемый вывод:**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### Шаг 5: Проверка Frontend

Откройте в браузере: http://localhost:5173

Должна открыться главная страница CryptoTracker.

**Если страница не загружается:**

1. Проверьте, что сервер запущен (см. вывод команды)
2. Проверьте консоль браузера (F12) на ошибки
3. Убедитесь, что порт 5173 не занят другим приложением

---

## Проверка работоспособности

### Тест 1: Регистрация пользователя

1. Откройте http://localhost:5173
2. Нажмите "Sign Up"
3. Введите email (например: `test@example.com`)
4. Введите пароль (минимум 8 символов, например: `Test1234`)
5. Подтвердите пароль
6. Нажмите "Create account"

**Ожидаемый результат:**
- Автоматический вход в систему
- Редирект на страницу Market Data
- В навигации отображается ваш email

**Проверка в БД:**
```bash
docker-compose exec postgres psql -U postgres -d cryptotracker -c "SELECT id, email, created_at FROM users;"
```

Должен отобразиться ваш новый пользователь.

### Тест 2: Просмотр Market Data

1. После входа перейдите в "Market Data"
2. Должна отобразиться таблица с криптовалютами

**Если таблица пустая:**
- Это нормально, если CoinMarketCap API не настроен
- Данные появятся после настройки API ключа и синхронизации

### Тест 3: Обновление до VIP

1. Перейдите в "Pricing"
2. Нажмите "Upgrade to VIP"
3. Подтвердите активацию

**Ожидаемый результат:**
- Появится сообщение об активации
- В навигации появятся ссылки на страницы сигналов
- Статус подписки изменится на "vip"

**Проверка в БД:**
```bash
docker-compose exec postgres psql -U postgres -d cryptotracker -c "SELECT user_id, plan, status FROM subscriptions;"
```

### Тест 4: Просмотр страниц сигналов (VIP)

После обновления до VIP должны быть доступны:
- MEXC Spot & Futures
- Funding Rate Spread
- MEXC & DEX Price Spread

**Ожидаемый результат:**
- Страницы открываются без ошибок
- Отображается сообщение "No signals found" (если сигналов нет)
- WebSocket подключается (проверьте консоль браузера F12)

### Тест 5: Настройки профиля

1. Перейдите в "Profile"
2. Измените настройки уведомлений
3. Сохраните изменения

**Ожидаемый результат:**
- Изменения сохраняются
- Настройки отображаются корректно

### Тест 6: API через документацию

1. Откройте http://localhost:8000/docs
2. Попробуйте выполнить запрос:
   - POST `/api/v1/auth/register` - регистрация
   - POST `/api/v1/auth/login` - вход
   - GET `/api/v1/users/me` - профиль (требует авторизации)

**Ожидаемый результат:**
- Swagger UI открывается
- Можно выполнить запросы
- Получить ответы от API

---

## Решение проблем

### Проблема 1: "ModuleNotFoundError" при запуске backend

**Причина:** Зависимости не установлены или виртуальное окружение не активировано

**Решение:**
```bash
# 1. Активируйте виртуальное окружение
# Windows: venv\Scripts\activate
# Linux/Mac: source venv/bin/activate

# 2. Переустановите зависимости
pip install -e ".[dev]"

# 3. Проверьте установку
pip list | grep fastapi
```

### Проблема 2: "Connection refused" к PostgreSQL

**Причина:** PostgreSQL не запущен или неправильный URL

**Решение:**
```bash
# 1. Проверьте статус
docker-compose ps

# 2. Если не запущен, запустите
docker-compose up -d postgres

# 3. Проверьте логи
docker-compose logs postgres

# 4. Проверьте DATABASE_URL в .env
```

### Проблема 3: "Port 8000 already in use"

**Причина:** Порт 8000 занят другим приложением

**Решение:**
```bash
# Windows - найти процесс
netstat -ano | findstr :8000
taskkill /PID <PID> /F

# Linux/Mac - найти процесс
lsof -i :8000
kill -9 <PID>

# Или измените порт в команде запуска
uvicorn app.main:app --reload --port 8001
```

### Проблема 4: "Port 5173 already in use" (Frontend)

**Решение:**
```bash
# Windows
netstat -ano | findstr :5173
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :5173
kill -9 <PID>

# Или измените порт в vite.config.ts
```

### Проблема 5: "Alembic migration failed"

**Причина:** База данных не готова или неправильные настройки

**Решение:**
```bash
# 1. Проверьте подключение к БД
docker-compose exec postgres psql -U postgres -d cryptotracker

# 2. Если БД не существует, создайте
docker-compose exec postgres psql -U postgres -c "CREATE DATABASE cryptotracker;"

# 3. Проверьте DATABASE_URL_SYNC в .env

# 4. Попробуйте миграции снова
alembic upgrade head
```

### Проблема 6: Frontend не подключается к Backend

**Причина:** Неправильный API URL или CORS

**Решение:**
1. Проверьте `frontend/.env` - должен быть `VITE_API_URL=http://localhost:8000`
2. Перезапустите frontend после изменения `.env`
3. Проверьте консоль браузера (F12) на ошибки CORS
4. Убедитесь, что backend запущен на порту 8000

### Проблема 7: WebSocket не подключается

**Причина:** Неправильный токен или нет VIP подписки

**Решение:**
1. Убедитесь, что пользователь имеет VIP подписку
2. Проверьте токен в localStorage браузера (F12 → Application → Local Storage)
3. Проверьте консоль браузера на ошибки WebSocket
4. Убедитесь, что backend запущен и WebSocket эндпоинт доступен

### Проблема 8: "npm install" ошибки

**Причина:** Проблемы с npm или сетью

**Решение:**
```bash
# Очистите кеш npm
npm cache clean --force

# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json  # Linux/Mac
rmdir /s node_modules & del package-lock.json  # Windows

# Переустановите
npm install

# Или используйте другой пакетный менеджер
pnpm install
# или
yarn install
```

### Проблема 9: Docker контейнеры не запускаются

**Решение:**
```bash
# 1. Проверьте статус Docker
docker ps

# 2. Перезапустите Docker Desktop (Windows/Mac)
# Или перезапустите Docker daemon (Linux)
sudo systemctl restart docker

# 3. Удалите и пересоздайте контейнеры
docker-compose down
docker-compose up -d postgres redis

# 4. Проверьте логи
docker-compose logs
```

### Проблема 10: Ошибки TypeScript в Frontend

**Решение:**
```bash
# 1. Проверьте версию TypeScript
npm list typescript

# 2. Переустановите зависимости
rm -rf node_modules package-lock.json
npm install

# 3. Проверьте tsconfig.json
# Убедитесь, что все пути правильные
```

---

## Дополнительная настройка

### Настройка CoinMarketCap API (опционально)

1. Зарегистрируйтесь на https://coinmarketcap.com/api/
2. Получите API ключ (Basic план бесплатный)
3. Добавьте в `.env`:
   ```env
   COINMARKETCAP_API_KEY=your-api-key-here
   ```
4. Перезапустите backend
5. Данные начнут синхронизироваться каждые 5 минут

### Настройка Telegram бота (опционально)

1. Создайте бота через @BotFather в Telegram
2. Получите токен бота
3. Добавьте бота в нужную группу
4. Получите Chat ID группы (используйте @userinfobot или другой способ)
5. Добавьте в `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=your-bot-token
   TELEGRAM_CHAT_ID=your-chat-id
   ```
6. Перезапустите backend
7. Бот начнет мониторить группу и создавать сигналы автоматически

### Настройка Email (SMTP) (опционально)

**Для Gmail:**

1. Включите двухфакторную аутентификацию
2. Создайте App Password: https://myaccount.google.com/apppasswords
3. Добавьте в `.env`:
   ```env
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASSWORD=your-app-password
   SMTP_FROM_EMAIL=noreply@cryptotracker.com
   ```
4. Перезапустите backend

**Для других провайдеров:**
- Измените `SMTP_HOST` и `SMTP_PORT` соответственно
- Используйте правильные учетные данные

---

## Проверочный чеклист

Перед демонстрацией убедитесь:

- [ ] Python 3.11+ установлен
- [ ] Node.js 18+ установлен
- [ ] Docker установлен и запущен
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости backend установлены (`pip install -e ".[dev]"`)
- [ ] Зависимости frontend установлены (`npm install`)
- [ ] Файл `.env` создан и заполнен (минимум SECRET_KEY)
- [ ] Docker контейнеры запущены (`docker-compose ps`)
- [ ] Миграции применены (`alembic upgrade head`)
- [ ] Backend запущен и доступен (http://localhost:8000)
- [ ] Frontend запущен и доступен (http://localhost:5173)
- [ ] Можно зарегистрироваться
- [ ] Можно войти
- [ ] Можно обновиться до VIP
- [ ] Страницы сигналов доступны для VIP
- [ ] WebSocket подключается (проверьте консоль браузера)

---

## Полезные команды

### Backend

```bash
# Запуск с перезагрузкой
uvicorn app.main:app --reload

# Запуск без перезагрузки (production-like)
uvicorn app.main:app --host 0.0.0.0 --port 8000

# Проверка кода
black app
flake8 app
mypy app

# Запуск тестов
pytest
```

### Frontend

```bash
# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Просмотр production сборки
npm run preview

# Линтинг
npm run lint
```

### Docker

```bash
# Запуск сервисов
docker-compose up -d

# Остановка сервисов
docker-compose down

# Просмотр логов
docker-compose logs -f

# Пересоздание контейнеров
docker-compose down
docker-compose up -d

# Очистка volumes (удалит все данные!)
docker-compose down -v
```

### База данных

```bash
# Подключение к PostgreSQL
docker-compose exec postgres psql -U postgres -d cryptotracker

# Просмотр таблиц
docker-compose exec postgres psql -U postgres -d cryptotracker -c "\dt"

# Просмотр пользователей
docker-compose exec postgres psql -U postgres -d cryptotracker -c "SELECT * FROM users;"

# Просмотр сигналов
docker-compose exec postgres psql -U postgres -d cryptotracker -c "SELECT * FROM signals_mexc_spot_futures LIMIT 5;"
```

---

## Структура терминалов для работы

### Терминал 1: Backend
```bash
# Активируйте venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# Запустите backend
uvicorn app.main:app --reload
```

### Терминал 2: Frontend
```bash
cd frontend
npm run dev
```

### Терминал 3: Docker (опционально)
```bash
# Для мониторинга логов
docker-compose logs -f
```

---

## Дополнительная помощь

Если возникли проблемы, проверьте:
1. Логи backend в терминале
2. Логи frontend в терминале
3. Консоль браузера (F12)
4. Логи Docker: `docker-compose logs`

### Полезные документы проекта

- `README.md` - общее описание проекта
- `DEMO_GUIDE.md` - руководство по демонстрации функциональности
- `WEBSOCKET_GUIDE.md` - документация по WebSocket API
- `DEPLOYMENT_READY.md` - инструкции по деплою в production
- `QUICKSTART.md` - краткая инструкция по быстрому старту

### Что делать после успешного запуска

1. **Зарегистрируйте первого пользователя** через веб-интерфейс
2. **Обновите до VIP** через страницу Pricing (это активирует все функции)
3. **Настройте внешние сервисы** (CoinMarketCap, Telegram, SMTP) для полной функциональности
4. **Изучите API документацию** на http://localhost:8000/docs
5. **Протестируйте WebSocket** подключение через страницы сигналов

### Следующие шаги разработки

После успешного локального запуска можно:
- Настроить CI/CD для автоматического тестирования
- Подготовить к деплою в production (см. `DEPLOYMENT_READY.md`)
- Добавить дополнительные функции согласно roadmap в ТЗ
- Настроить мониторинг и логирование

---

## Готово! 🎉

Если все шаги выполнены успешно, проект должен работать полностью.

**Доступ:**
- Frontend: http://localhost:5173
- Backend: http://localhost:8000
- API Docs: http://localhost:8000/docs
- Health Check: http://localhost:8000/health

**Следующие шаги:**
1. Зарегистрируйтесь
2. Обновитесь до VIP
3. Настройте внешние сервисы (CoinMarketCap, Telegram, SMTP) для полной функциональности

**Удачи в разработке!** 🚀

